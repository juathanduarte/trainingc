#include <stdio.h>
#include <stdlib.h>

// Parâmetros da função

int main(){
    //Sempre definir o TIPO DO PARÂMETRO
    float raiz(float r);
    int soma(int x, int y);
    int exponencial(float a, int b);
    
    //Também podemos definir funções SEM PARÂMETRO
    float leitura();
    int le_aquivo(void);

    system("pause");
    return 0;
}